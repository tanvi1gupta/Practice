find the sqaure-root/cube-root of a number
given an array,find a triplet a[i], a[j], a[k] such that a[i]<a[j]<a[k] && i<j<k [[O(n) space & O(1) space]]
given a string find the longest palindrome substring
given a random stream of characters, and a lookup dictionary, find whether the stream can be broken down into valid set of words
If Q4, then output one valid sentence
stock problem,..leetcode ...given an array, where a[i] tells you the price of the stock for that day, find the best times to buy and sell the stock
given a binary tree and a node, find all the nodes at distance k from that node
design the snake and ladder problem.
merge k sorted arrays
given sorted arrays, find the median of the merged arrays, without actually merging the array
      http://leetcode.com/2011/03/median-of-two-sorted-arrays.html
histogram problem, given an array, where a[i] depicts the height of a tower, find the maximum rectangle that can be constructed
      http://www.geeksforgeeks.org/largest-rectangle-under-histogram/ 
given a matrix made of 0s and 1s, find the area of rectangle with max number of 0s
      https://github.com/tanvi1gupta/Practice/blob/master/ZeroOnemaxArea.cpp
determine whether 2 rectangles are overlapping or not
Given a variety of opening and closing braces "({[ ]})" output whether the braes form a valid set or not
       https://www.interviewcake.com/question/bracket-validator
given 2 words, and a dictionary , find the minimum number of transformations that should be made to change start to end, 
changing one character at a time
Given:
start = "hit"
end = "cog"
dict = ["hot","dot","dog","lot","log"]
As one shortest transformation is "hit" -> "hot" -> "dot" -> "dog" -> "cog",
return its length 5.
Design LRU Cache
find a number in a rotated sorted matrix.
Given a triangle, find the minimum path sum from top to bottom. Each step you may move to adjacent numbers on the row below.
[
     [2],
    [3,4],
   [6,5,7],
  [4,1,8,3]
]  Solution is 2->3->5->1

Right view of the tree
Reverse a Circular LL. ?? if they say yes, then ask, can we actually reverse a  circular LL 

given 2 robots, and 4 methods, GoLeft, GoRight, OnParachute, Stay, the 2 robots are ejected from a plane and land on earth, 
now you need to write a code which makes the 2 robots meet at a position. 

10 steps go left,
20 go right
30 go left
40 go right
if on parachute stay else go left 50
60 go right 
